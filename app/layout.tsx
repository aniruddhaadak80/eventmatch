import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "EventMatch | AI-Powered Event Discovery",
  description: "Discover concerts, conferences, workshops, and experiences tailored to your interests. Chat with our AI to find your perfect event.",
  keywords: ["events", "discover", "AI", "concerts", "conferences", "workshops", "local events"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
